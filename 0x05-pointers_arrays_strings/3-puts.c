#include "main.h"

/**
 * _puts - Returns the string length of parameter value
 * @str: char pointer
 * Return: void
 */

void _puts(char *str)
{
	int c = 0;

	while (s[c] != '\0')
	{
		_putchar(s[c]);
		c++;
	}
	_putchar('\n');
}
