#include <stdio.h>

/**
 * main - prints the various sizes of data types on the computer
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

	printf(sizeof(char));
	printf(sizeof(int));
	printf(sizeof(long));
	printf(sizeof(float));
	return (0);
}
